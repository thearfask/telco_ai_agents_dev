[project]
name = "telco-ai-investigator"
version = "0.1.0"
description = "Agentic telco incident investigation using LangGraph"
requires-python = ">=3.12"

dependencies = [
    "duckdb",
    "langchain-core",
    "langchain-openai",
    "langgraph",
    "openai",
    "pydantic",
    "streamlit",
]

[tool.uv]
package = false